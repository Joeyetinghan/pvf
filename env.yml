name: pvf
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      # Core libraries actually used by the codebase
      - numpy==2.2.6
      - torch==2.5.1
      - transformers==4.46.2
      - datasets==4.6.1
      - pillow==12.1.1
      - tqdm==4.67.3
      - omegaconf
